import { Context } from "./context.js";
import { RenderBuffer } from "./renderbuffer.js";
import { Texture2D } from "./texture.js";
export declare class FrameBuffer {
    readonly context: Context;
    readonly target: 36160;
    readonly glFrameBuffer: WebGLFramebuffer;
    private _colorBuffer;
    private _depthBuffer;
    constructor(context: Context);
    delete(): void;
    bind(): void;
    unbind(): void;
    check(): void;
    get colorBuffer(): RenderBuffer | Texture2D | null;
    set colorBuffer(buffer: RenderBuffer | Texture2D | null);
    get depthBuffer(): RenderBuffer | Texture2D | null;
    set depthBuffer(buffer: RenderBuffer | Texture2D | null);
    private set;
}
