import * as tokens from "./tokens.js";
import * as utils from "./utils.js";
export class Grammar {
    constructor(start) {
        this.start = start;
        this.symbols = this.start.accept(new SymbolsCollector(), "");
        this.optionality = this.runIteratively(new OptionalityDeriver(), (o1, o2) => o1 === o2);
        this.firstSets = this.runIteratively(new FirstSetDeriver(this.optionality), equalSets);
        this.followSets = this.runIteratively(new FollowSetDeriver(this.optionality, this.firstSets), equalSets, set(tokens.eof), true);
    }
    /*
     * Deriving the optionality, first tokens, and follow tokens of grammar symbols, on a high level, share a common iterative
     * process that resolves any circular or recursive dependencies between these symbols. It is based on repeating a derivation
     * a few times, feeding the output of each iteration into the input of the next iteration, until no changes are seen between
     * the output and input. This method implements that iterative process.
     */
    runIteratively(visitor, equality, startSymbolInitialResult = undefined, mutatesInputAndReturnsPrevInput = false) {
        let result = startSymbolInitialResult !== undefined
            ? map([this.start, startSymbolInitialResult])
            : map();
        let changed = true;
        while (changed) {
            const nextResult = this.derive(visitor, result);
            changed = this.compare(result, nextResult, equality);
            if (!mutatesInputAndReturnsPrevInput) {
                result = nextResult;
            }
        }
        return result;
    }
    derive(visitor, result) {
        const nextResult = new Map();
        for (const [s, _] of this.symbols) {
            nextResult.set(s, s.accept(visitor, result));
        }
        return nextResult;
    }
    compare(result, nextResult, equality) {
        let changed = false;
        for (const [s, _] of this.symbols) {
            const r = result.get(s);
            const n = nextResult.get(s);
            changed || (changed = r === undefined || n === undefined || !equality(r, n));
        }
        return changed;
    }
    random() {
        return this.start.random();
    }
    isOptional(symbol) {
        return get(this.optionality, symbol);
    }
    firstSetOf(symbol) {
        return get(this.firstSets, symbol);
    }
    followSetOf(symbol) {
        return get(this.followSets, symbol);
    }
    ll1EligiblityProblems() {
        const visitor = new LL1EligibilityVerifier(this.firstSets, this.followSets);
        return [...this.symbols].flatMap(([s, p]) => s.accept(visitor, p));
    }
}
export function terminals(definitions) {
    const result = {};
    for (const key in definitions) {
        result[key] = terminal(definitions[key]);
    }
    return result;
}
export function terminal(tokenType) {
    return new TerminalImpl(tokenType);
}
export function choice(productions) {
    return typeof productions === "function"
        ? new RecursiveImpl(() => new ChoiceImpl(productions))
        : new ChoiceImpl(() => productions);
}
export function production(definition, order = undefined) {
    return typeof definition === "function"
        ? new RecursiveImpl(() => new ProductionImpl(definition, order))
        : new ProductionImpl(() => definition, order);
}
class SymbolImpl {
    constructor() {
        this.randomExpression = new utils.Lazy(() => this.asRandomExpression());
    }
    random() {
        return this.randomExpression.value.value;
    }
}
class NonRepeatableImpl extends SymbolImpl {
    mapped(toMapper, fromMapper) {
        return new MappedNonRepeatableImpl(this, toMapper, fromMapper);
    }
}
class RepeatableImpl extends SymbolImpl {
    optional(nestingResistance = 0.125) {
        return new OptionalImpl(this, nestingResistance);
    }
    zeroOrMore(nestingResistance = 0.125) {
        return zeroOrMore(this, nestingResistance);
    }
    oneOrMore(nestingResistance = 0.125) {
        return oneOrMore(this, nestingResistance);
    }
    mapped(toMapper, fromMapper) {
        return new MappedRepeatableImpl(this, toMapper, fromMapper);
    }
}
class OptionalImpl extends NonRepeatableImpl {
    constructor(symbol, nestingResistance) {
        super();
        this.symbol = symbol;
        this.nestingResistance = nestingResistance;
        this.size = this.symbol.size + 1;
    }
    accept(visitor, input) {
        return visitor.visitOptional(this, input);
    }
    asRandomExpression(depth = 0) {
        const dice = Math.random() * this.size;
        return (dice < this.symbol.size * (2 ** (-depth * this.nestingResistance))
            ? this.symbol.asRandomExpression(depth)
            : utils.Expression.of(null));
    }
    *tokens(value) {
        if (value !== null) {
            for (const t of this.symbol.tokens(value)) {
                yield t;
            }
        }
        ;
    }
}
class TerminalImpl extends RepeatableImpl {
    constructor(tokenType) {
        super();
        this.tokenType = tokenType;
        this.size = 1;
    }
    accept(visitor, input) {
        return visitor.visitTerminal(this, input);
    }
    asRandomExpression() {
        return utils.Expression.of(this.token(this.tokenType.pattern.randomString(0.125)));
    }
    *tokens(value) {
        yield value;
    }
    tokenless() {
        return this.mapped(t => t.value, v => this.token(this.tokenType.stringify(v)));
    }
    token(lexme, position = { line: 0, column: 0, index: 0 }) {
        return this.tokenType.token(lexme, position);
    }
}
class ChoiceImpl extends RepeatableImpl {
    constructor(productionsSupplier) {
        super();
        this.productionsSupplier = productionsSupplier;
        this._productions = new utils.Lazy(this.productionsSupplier);
    }
    get productions() {
        return this._productions.value;
    }
    get size() {
        return Object.values(this.productions).map(p => p.size).reduce((a, b) => a + b, 0);
    }
    accept(visitor, input) {
        return visitor.visitChoice(this, input);
    }
    asRandomExpression(depth = 0) {
        let dice = utils.randomInt(this.size);
        let i = 0;
        const productions = Object.entries(this.productions);
        while (dice >= productions[i][1].size) {
            dice -= productions[i++][1].size;
        }
        return productions[i][1].asRandomExpression(depth)
            .then(value => ({ type: productions[i][0], value }));
    }
    *tokens(value) {
        for (const [type, production] of Object.entries(this.productions)) {
            if (type === value.type) {
                for (const token of production.tokens(value.value)) {
                    yield token;
                }
            }
        }
    }
}
class ProductionImpl extends RepeatableImpl {
    constructor(definitionSupplier, customOrder = undefined) {
        super();
        this.definitionSupplier = definitionSupplier;
        this.customOrder = customOrder;
        this._definition = new utils.Lazy(this.definitionSupplier);
        this._order = new utils.Lazy(() => this.customOrder !== undefined ? this.customOrder : Object.keys(this.definition));
    }
    get definition() {
        return this._definition.value;
    }
    get order() {
        return this._order.value;
    }
    get size() {
        return this.order.map(k => this.definition[k].size).reduce((a, b) => a * b, 1);
    }
    accept(visitor, input) {
        return visitor.visitProduction(this, input);
    }
    asRandomExpression(depth = 0) {
        let resultPromise = utils.Expression.of({});
        for (const key of this.order) {
            resultPromise = resultPromise.then(result => this.definition[key]
                .asRandomExpression(depth + 1)
                .then(setInto(result, key)));
        }
        return resultPromise;
    }
    *tokens(value) {
        for (const key of this.order) {
            if (key in value) {
                for (const t of this.definition[key].tokens(value[key])) {
                    yield t;
                }
            }
        }
        ;
    }
}
class RecursiveImpl extends RepeatableImpl {
    constructor(symbolSupplier) {
        super();
        this.size = 1;
        this.symbol = symbolSupplier(this);
    }
    accept(visitor, input) {
        return visitor.visitRecursive(this, input);
    }
    asRandomExpression(depth = 0) {
        return this.symbol.asRandomExpression(depth);
    }
    tokens(value) {
        return this.symbol.tokens(value);
    }
}
class MappedNonRepeatableImpl extends NonRepeatableImpl {
    constructor(symbol, toMapper, fromMapper) {
        super();
        this.symbol = symbol;
        this.toMapper = toMapper;
        this.fromMapper = fromMapper;
        this.size = this.symbol.size;
    }
    accept(visitor, input) {
        return visitor.visitMappedNonRepeatable(this, input);
    }
    asRandomExpression(depth = 0) {
        return this.symbol.asRandomExpression(depth).then(this.toMapper);
    }
    tokens(value) {
        return this.symbol.tokens(this.fromMapper(value));
    }
}
class MappedRepeatableImpl extends RepeatableImpl {
    constructor(symbol, toMapper, fromMapper) {
        super();
        this.symbol = symbol;
        this.toMapper = toMapper;
        this.fromMapper = fromMapper;
        this.size = this.symbol.size;
    }
    accept(visitor, input) {
        return visitor.visitMappedRepeatable(this, input);
    }
    asRandomExpression(depth) {
        return this.symbol.asRandomExpression(depth).then(this.toMapper);
    }
    tokens(value) {
        return this.symbol.tokens(this.fromMapper(value));
    }
}
class SymbolsCollector {
    constructor() {
        this.map = new Map();
    }
    visitTerminal(symbol, path) {
        return this.map.set(symbol, path);
    }
    visitOptional(symbol, path) {
        return this.addChilds([["?", symbol.symbol]], symbol, path);
    }
    visitChoice(symbol, path) {
        return this.addChilds(Object.entries(symbol.productions), symbol, path);
    }
    visitProduction(symbol, path) {
        return this.addChilds(Object.entries(symbol.definition), symbol, path);
    }
    visitRecursive(symbol, path) {
        return this.map.has(symbol) ? this.map : this.addChilds([["*", symbol.symbol]], symbol, path);
    }
    visitMappedNonRepeatable(symbol, path) {
        return this.addChilds([["~", symbol.symbol]], symbol, path);
    }
    visitMappedRepeatable(symbol, path) {
        return this.addChilds([["~", symbol.symbol]], symbol, path);
    }
    addChilds(keyChildTuples, parent, path) {
        const result = this.map.set(parent, path);
        keyChildTuples.forEach(([key, child]) => child.accept(this, path + "/" + key));
        return result;
    }
}
class OptionalityDeriver {
    visitTerminal() {
        return false;
    }
    visitOptional() {
        return true;
    }
    visitChoice(symbol, input) {
        return Object.values(symbol.productions).reduce((a, p) => this.get(p, input) || a, false);
    }
    visitProduction(symbol, input) {
        return symbol.order.reduce((a, k) => this.get(symbol.definition[k], input) && a, true);
    }
    visitRecursive(symbol, input) {
        return this.get(symbol.symbol, input);
    }
    visitMappedNonRepeatable(symbol, input) {
        return this.get(symbol.symbol, input);
    }
    visitMappedRepeatable(symbol, input) {
        return this.get(symbol.symbol, input);
    }
    get(symbol, input) {
        return input.get(symbol) ?? true;
    }
}
class FirstSetDeriver {
    constructor(optionality) {
        this.optionality = optionality;
    }
    visitTerminal(symbol) {
        return new Set([symbol.tokenType]);
    }
    visitOptional(symbol, input) {
        return this.get(symbol.symbol, input);
    }
    visitChoice(symbol, input) {
        return Object.values(symbol.productions)
            .map(p => this.get(p, input))
            .reduce(merge, new Set());
    }
    visitProduction(symbol, input) {
        const firstNonOptional = symbol.order.findIndex(k => !this.optionality.get(symbol.definition[k]));
        return symbol.order
            .map((k, i) => [this.get(symbol.definition[k], input), i])
            .filter(([_, i]) => i <= firstNonOptional)
            .reduce((set, [s, _]) => merge(set, s), new Set());
    }
    visitRecursive(symbol, input) {
        return this.get(symbol.symbol, input);
    }
    visitMappedNonRepeatable(symbol, input) {
        return this.get(symbol.symbol, input);
    }
    visitMappedRepeatable(symbol, input) {
        return this.get(symbol.symbol, input);
    }
    get(symbol, input) {
        return input.get(symbol) ?? new Set();
    }
}
class FollowSetDeriver {
    constructor(optionality, firstSets) {
        this.optionality = optionality;
        this.firstSets = firstSets;
    }
    visitTerminal(symbol, input) {
        return this.get(symbol, input);
    }
    visitOptional(symbol, input) {
        return this.pass(this.get(symbol, input), symbol.symbol, input);
    }
    visitChoice(symbol, input) {
        return Object.values(symbol.productions).reduce((r, p) => this.pass(r, p, input), this.get(symbol, input));
    }
    visitProduction(symbol, input) {
        const result = this.get(symbol, input);
        let followSet = result;
        for (let i = symbol.order.length - 1; i >= 0; i--) {
            const key = symbol.order[i];
            const s = symbol.definition[key];
            this.pass(followSet, s, input);
            const nextFirstSet = this.get(s, this.firstSets);
            followSet = this.optionality.get(s) ? merge(followSet, nextFirstSet) : nextFirstSet;
        }
        return result;
    }
    visitRecursive(symbol, input) {
        return this.pass(this.get(symbol, input), symbol.symbol, input);
    }
    visitMappedNonRepeatable(symbol, input) {
        return this.pass(this.get(symbol, input), symbol.symbol, input);
    }
    visitMappedRepeatable(symbol, input) {
        return this.pass(this.get(symbol, input), symbol.symbol, input);
    }
    pass(result, child, input) {
        input.set(child, merge(result, this.get(child, input)));
        return result;
    }
    get(symbol, input) {
        return input.get(symbol) ?? new Set();
    }
}
class LL1EligibilityVerifier {
    constructor(firstSets, followSets) {
        this.firstSets = firstSets;
        this.followSets = followSets;
    }
    visitTerminal() {
        return [];
    }
    visitOptional(symbol, path) {
        const output = [];
        const firstSet = this.firstSets.get(symbol);
        const followSet = this.followSets.get(symbol);
        for (const token of this.tokensOf(firstSet)) {
            if (this.isIn(followSet, token)) {
                output.push(path + ": first and follow set overlap");
                break;
            }
        }
        return output;
    }
    visitChoice(symbol, path) {
        const output = [];
        const flatFirstSet = Object.values(symbol.productions).map(p => this.firstSets.get(p)).flatMap(s => [...this.tokensOf(s)]);
        const firstSet = this.firstSets.get(symbol);
        const firstSetSize = firstSet !== undefined ? firstSet.size : 0;
        if (flatFirstSet.length > firstSetSize) {
            output.push(path + ": first sets overlap");
        }
        return output;
    }
    visitProduction() {
        return [];
    }
    visitRecursive() {
        return [];
    }
    visitMappedNonRepeatable() {
        return [];
    }
    visitMappedRepeatable() {
        return [];
    }
    *tokensOf(set) {
        if (set === undefined) {
            return;
        }
        for (const token of set) {
            yield token;
        }
    }
    isIn(set, token) {
        return set !== undefined ? set.has(token) : false;
    }
}
function zeroOrMore(symbol, nestingResistance) {
    return listProductions(symbol, nestingResistance).list.mapped(n => toList(n), l => toLinkedList(l));
}
function oneOrMore(symbol, nestingResistance) {
    return listProductions(symbol, nestingResistance).con.mapped(n => toNonEmptyList(n), l => toCon(l));
}
function listProductions(symbol, nestingResistance) {
    const con = production(() => ({ head: symbol, tail: list }));
    const list = con.optional(nestingResistance);
    return { list, con };
}
function toList(n) {
    const result = [];
    while (n !== null) {
        result.push(n.head);
        n = n.tail;
    }
    return result;
}
function toNonEmptyList(n) {
    return [n.head, ...toList(n.tail)];
}
function toLinkedList(l) {
    let tail = null;
    for (const head of [...l].reverse()) {
        tail = { head, tail };
    }
    return tail;
}
function toCon(l) {
    return toLinkedList(l);
}
function set(...items) {
    return new Set(items);
}
function map(...entries) {
    return new Map(entries);
}
function get(map, symbol) {
    return map.get(symbol) ?? notFound(symbol);
}
function notFound(symbol) {
    throw new Error("Symbol not found: " + symbol);
}
function merge(s1, s2) {
    return new Set([...s1, ...s2]);
}
function equalSets(s1, s2) {
    return s1.size === s2.size && [...s1].every(v => s2.has(v));
}
function setInto(parent, key) {
    return child => {
        parent[key] = child;
        return parent;
    };
}
