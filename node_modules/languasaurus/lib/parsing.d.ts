import * as gram from "./grammar.js";
import * as lex from "./scanning.js";
import * as tokens from "./tokens.js";
import { TextInputStream } from "./streams.js";
import { Consumer } from "./utils.js";
export declare function recursiveDescentParser<T, D extends lex.TokenDefinitions>(tokenDefinitions: D, start: gram.Symbol<T>, errorReporterFactory?: (scanner: lex.Scanner<any>) => ParsingErrorReporter, whitespace?: Set<tokens.TokenType<any>> | undefined): (input: TextInputStream) => T;
export interface ParsingErrorReporter {
    invalidToken(errorToken: tokens.Token<any>): void;
    unexpectedToken(errorToken: tokens.Token<any>, ...expected: Set<tokens.TokenType<any>>[]): void;
}
export declare class DefaultParsingErrorReporter implements ParsingErrorReporter {
    private scanner;
    private errorConsumer;
    constructor(scanner: lex.Scanner<any>, errorConsumer?: Consumer<string>);
    invalidToken(errorToken: tokens.Token<any>): void;
    unexpectedToken(errorToken: tokens.Token<any>, ...expected: Set<tokens.TokenType<any>>[]): void;
    private name;
}
export declare class LookAhead {
    private iterator;
    private whiteSpaceTokens;
    private errorReporter;
    private _current;
    constructor(iterator: Generator<tokens.Token<any>, any, tokens.Token<any>>, whiteSpaceTokens: Set<tokens.TokenType<any>>, errorReporter: (errorToken: tokens.Token<any>) => void);
    get current(): tokens.Token<any>;
    advance(): tokens.Token<any>;
    private next;
}
export interface ParsingVisitor {
    visitTerminal<T>(symbol: gram.Terminal<T>, input: LookAhead): tokens.Token<T>;
    visitOptional<T>(acceptor: ParsingVisitorAcceptor, symbol: gram.Optional<T>, input: LookAhead): T | null;
    visitChoice<P extends gram.Definition>(acceptor: ParsingVisitorAcceptor, symbol: gram.Choice<P>, input: LookAhead): gram.Cases<P>;
    visitProduction<D extends gram.Definition>(acceptor: ParsingVisitorAcceptor, symbol: gram.Production<D>, input: LookAhead): gram.Structure<D>;
    visitLazy<T>(acceptor: ParsingVisitorAcceptor, symbol: gram.Recursive<T>, input: LookAhead): T;
    visitMappedNonRepeatable<S, T>(acceptor: ParsingVisitorAcceptor, symbol: gram.MappedNonRepeatable<S, T>, input: LookAhead): T;
    visitMappedRepeatable<S, T>(acceptor: ParsingVisitorAcceptor, symbol: gram.MappedRepeatable<S, T>, input: LookAhead): T;
}
export interface ParsingVisitorAcceptor {
    accept<T>(symbol: gram.Symbol<T>, input: LookAhead): T;
}
export declare class RecursiveDescentParsing implements gram.Visitor<LookAhead, any>, ParsingVisitorAcceptor {
    private grammar;
    private errorReporter;
    private visitor;
    constructor(grammar: gram.Grammar<any>, errorReporter: ParsingErrorReporter);
    accept<T>(symbol: gram.Symbol<T>, input: LookAhead): T;
    visitTerminal<T>(symbol: gram.Terminal<T>, input: LookAhead): tokens.Token<T>;
    visitOptional<T>(symbol: gram.Optional<T>, input: LookAhead): T | null;
    visitChoice<P extends gram.Definition>(symbol: gram.Choice<P>, input: LookAhead): gram.Cases<P>;
    visitProduction<D extends gram.Definition>(symbol: gram.Production<D>, input: LookAhead): gram.Structure<D>;
    visitRecursive<S>(symbol: gram.Recursive<S>, input: LookAhead): S;
    visitMappedNonRepeatable<S, T>(symbol: gram.MappedNonRepeatable<S, T>, input: LookAhead): T;
    visitMappedRepeatable<S, T>(symbol: gram.MappedRepeatable<S, T>, input: LookAhead): T;
}
export declare class RecursiveDescentParsingVisitor implements ParsingVisitor {
    private grammar;
    private errorReporter;
    constructor(grammar: gram.Grammar<any>, errorReporter: ParsingErrorReporter);
    private mustSkip;
    visitTerminal<T>(symbol: gram.Terminal<T>, input: LookAhead): tokens.Token<T>;
    visitOptional<T>(acceptor: ParsingVisitorAcceptor, symbol: gram.Optional<T>, input: LookAhead): T | null;
    visitChoice<P extends gram.Definition>(acceptor: ParsingVisitorAcceptor, symbol: gram.Choice<P>, input: LookAhead): gram.Cases<P>;
    visitProduction<D extends gram.Definition>(acceptor: ParsingVisitorAcceptor, symbol: gram.Production<D>, input: LookAhead): gram.Structure<D>;
    visitLazy<T>(acceptor: ParsingVisitorAcceptor, symbol: gram.Recursive<T>, input: LookAhead): T;
    visitMappedNonRepeatable<S, T>(acceptor: ParsingVisitorAcceptor, symbol: gram.MappedNonRepeatable<S, T>, input: LookAhead): T;
    visitMappedRepeatable<S, T>(acceptor: ParsingVisitorAcceptor, symbol: gram.MappedRepeatable<S, T>, input: LookAhead): T;
}
