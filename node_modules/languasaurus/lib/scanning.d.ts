import * as streams from './streams.js';
import * as tokens from './tokens.js';
export type TokenDefinitions = Record<string, tokens.TokenType<any>> & {
    EOF?: never;
    ERROR?: never;
};
export declare class Scanner<D extends TokenDefinitions> {
    private readonly tokenTypes;
    private readonly tokenTypesMap;
    private readonly automaton;
    private tokenDefinitions;
    constructor(tokenDefinitions: D);
    protected tieBreak(tokensTypes: tokens.TokenType<any>[]): tokens.TokenType<any>;
    get tokenTypeNames(): (keyof D | "ERROR" | "EOF")[];
    token(name: keyof D | "ERROR" | "EOF"): tokens.TokenType<any>;
    tokenTypeName<T>(tokenType: tokens.TokenType<T>): keyof D | "ERROR" | "EOF";
    iterator(stream: streams.InputStream<number>): Generator<tokens.Token<any>, void, unknown>;
    nextToken(stream: streams.InputStream<number>): tokens.Token<any>;
    randomToken(shortness?: number): tokens.Token<any>;
    private next;
}
